# MySQL
DB_PASSWORD=change_me_in_production

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

# AI service (set to host.docker.internal for Docker Desktop, host-gateway for Linux)
AI_SERVICE_URL=http://host.docker.internal:8001

# API Key (root key for admin operations)
API_KEY=change_me_in_production

# Server port
SERVER_PORT=8080
