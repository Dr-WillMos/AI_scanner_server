# 待补全名录

## P0 级缺失（阻塞上线，必须解决）

这些是系统稳定性和安全性的底线，不解决无法面向真实用户。

### 1. ~~API 限流与防刷~~

- **现状**：完全缺失。任何知道API地址和Key的人，可以无限制上传视频，打爆AI服务和带宽。
- **需完成**：
  - **接口限流**：基于 `deviceId` 或 `client IP`，对 `/detect` 接口实施限流（例如：每分钟3次，每小时10次）。Spring Boot 可集成 Bucket4j 或 Resilience4j。
  - **全局限流**：对 `/history` 等查询接口也做限流，防止恶意遍历。
  - **API Key 轮换与撤销**：目前是静态Key，需要支持后台动态生成、撤销、按设备分配不同Key。

### 2. ~~视频上传的稳定性和健壮性~~

- **现状**：客户端上传完整5秒MP4，后端接收后调用Python AI。网络错误处理基本为0。
- **需完成**：
  - **断点续传与重试**：移动网络不稳定，10MB视频上传易失败。需要客户端实现分片上传和自动重试（指数退避）。
  - **压缩与格式转换**：客户端录制的高码率H.264应强制压缩（分辨率降为480p或720p，码率1-2Mbps），减少流量和带宽压力。
  - **上传前校验**：客户端应检查视频文件是否损坏、时长是否为5秒。
  - **后端异步处理**：`/detect` 接口不应同步等待AI模型（Python服务可能耗时10秒+）。应改为：`POST /detect` 立即返回 `taskId`（202 Accepted），客户端轮询 `GET /task/{taskId}` 获取结果。这是最关键的架构改动。

### 3. Android 高版本与厂商兼容性

- **现状**：实现了MediaProjection和前台服务，但文档称“已攻克兼容性问题”，实际可能过于乐观。
- **需完成**：
  - **Android 14+ 的媒体投影权限**：用户每次授予权限后，令牌可能一次有效或一段时间有效。需要大量真机测试（小米、华为、OV、三星）。
  - **悬浮窗权限的国产机适配**：不同厂商（MIUI、ColorOS、HarmonyOS）对“后台弹出界面”和“悬浮窗”权限的管理策略不同，需要针对性适配和用户引导文案。
  - **低端机性能**：5秒录制+编码+上传，对低端CPU（如骁龙660）和IO是巨大挑战，可能导致应用卡顿或ANR。需要在真机上实测并增加“流畅模式”（降低分辨率/fps）。

### 4. 短信紧急响应的风险

- **现状**：高风险时自动发送短信。
- **需完成**：
  - **防短信轰炸**：同一设备/作者ID在短时间内（如10分钟）触发多次高风险，应只发送1次短信。否则老人连刷10个诈骗视频，会瞬间发出10条短信，可能被运营商拦截或导致费用问题。
  - **用户确认机制**：自动发送短信前，应在客户端弹窗让用户二次确认（“检测到极高风险，将通知您的紧急联系人，是否发送？”）。

---

## P1 级缺失（严重影响体验和运营，上线后必须快速迭代）

### 1. 完整的管理后台（运营端）

- **现状**：只有API接口，没有任何管理界面。黑名单需要手动调API添加。
- **需完成**：
  - **风险大盘**：实时检测量、高风险趋势、热门风险作者ID、后端/Python AI服务健康状态。
  - **黑名单管理界面**：可视化增删改查权威/全局黑名单，查看临时黑名单及剩余时间。
  - **检测记录审计**：按设备ID、作者ID、时间、风险等级搜索历史记录，并能够播放原视频（需要把视频文件从客户端或临时目录集中存储到OSS）。

### 2. 用户体系与隐私合规

- **现状**：只用 `deviceId` 标识用户，无账号、无隐私政策。
- **需完成**：
  - **隐私政策弹窗**：首次启动必须展示隐私政策，明确告知录屏、上传、分析、短信权限的使用目的。用户不同意则无法使用核心功能。
  - **用户数据删除机制**：需要支持用户请求删除其所有检测历史记录和关联的 `deviceId`。
  - （可选）**简单的手机号注册/登录**：为未来多设备同步、家庭成员绑定做基础。

### 3. 客户端历史记录的远程拉取和展示优化

- **现状**：有本地Room + 远程同步，但分页用 `afterId`，偏向技术实现。
- **需完成**：
  - **历史记录按日期分组展示**：目前是简单的列表，不直观。
  - **结果详情页优化**：需要让老人看懂“为什么是风险”。展示AI给出的关键词（如“视频中出现‘转账’、‘高收益’等词汇”）和对应截图（关键帧），而非原始 `raw_ai_result`。
  - **同步冲突处理**：本地和远程都有记录，`afterId` 分页可能导致新插入记录无法被拉取。需要更健壮的增量同步策略（如使用 `updated_at` 时间戳）。

### 4. 网络诊断与用户指引

- **现状**：有网络诊断工具，但藏得深。
- **需完成**：
  - **在检测失败时自动诊断并提示**：例如“上传失败：当前为移动数据网络，请连接WiFi或到设置中开启‘移动数据上传’”。直接把解决路径告诉老人，而不是给一个“网络诊断”按钮让他们自己点。

---

## P2 级缺失（锦上添花，但有更好）

### 1. 视频分析结果缓存

- **现状**：每次上传视频都调用AI。
- **需完成**：对同一个 `authorId` + 同一个视频内容的MD5，短时间内（如1小时）重复检测应直接返回缓存结果，避免浪费AI算力。初稿中提到“低频关键帧采样”本身就是一个很好的缓存Key思路。

### 2. 更丰富的预警方式

- **现状**：TTS、短信、弹窗。
- **需完成**：
  - **振动提醒**：高风险时配合振动，对老年用户更友好。
  - **系统通知栏推送**：当APP在后台时，通过Notification展示风险结果。

### 3. 客户端性能监控与埋点

- **现状**：无。
- **需完成**：接入简单的性能监控（如友盟+、Bugly），跟踪关键事件：
  - 录屏成功率、平均耗时
  - 上传成功率、平均耗时、平均文件大小
  - 各风险等级命中率
  - 崩溃率、ANR率