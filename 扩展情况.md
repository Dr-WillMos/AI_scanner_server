# 扩展情况 — 已实现功能说明

## 项目概述

短视频反诈后端（Short Video Anti-Fraud Backend），基于 Spring Boot 3.5.14 / Java 25，接收短视频并通过外部 Python AI 服务进行风险分析，计算风险等级，管理高风险发布者黑名单。

---

## 一、核心检测流水线

### 1.1 视频上传与风险检测

- **接口：** `POST /api/v1/detect`（multipart/form-data）
- **入参：** `deviceId`（设备ID）、`authorId`（作者ID）、`video`（视频文件，最大 10MB）
- **检测流程：**
  1. Redis 三层黑名单检查（权威 > 全局 > 临时），命中则直接返回 HIGH，不调用 AI，不写入数据库
  2. 调用 Python AI 服务 `POST /v1/analyze/video` 进行视频分析
  3. 通过加权公式计算风险分数
  4. 结果持久化到 MySQL `t_detection_record` 表
  5. 若判定为 HIGH 风险，自动将作者加入临时黑名单（24小时 TTL）
  6. 返回 `DetectResponse`

### 1.2 风险评分引擎（RiskCalculator）

- **三种风险等级：** `HIGH`（高）、`MEDIUM`（中）、`SAFE`（安全）
- **关键词命中规则：** `keywordHit == true` → 直接 HIGH，score = 1.0
- **加权评分公式：** `score = 0.7 × aiGlitchProb + 0.3 × violenceProb`
  - `score ≥ 0.6` → HIGH
  - `0.3 ≤ score < 0.6` → MEDIUM
  - `score < 0.3` → SAFE

### 1.3 Python AI 服务客户端（AiClient）

- 通过独立的 `aiRestClient` Bean 与外部 AI 服务通信
- 视频以 `ByteArrayResource` 字节流形式发送，保留原始文件名
- 反序列化 AI 返回的 JSON：`aiGlitchProb`、`violenceProb`、`transcription`、`keywordHit`
- 统一异常包装为 `RuntimeException`

---

## 二、三层黑名单系统

全部基于 Redis 实现，通过 `StringRedisTemplate` 操作。

### 2.1 权威黑名单（authority）
| 项目 | 说明 |
|------|------|
| Redis Key | `blacklist:authority`（Set 结构） |
| 数据来源 | 平台官方公告，但名单来源未实装 |
| 有效期 | 永久 |
| 优先级 | 最高（首先检查） |
| CRUD | `GET/POST/DELETE /api/v1/blacklist/authority` |
| 单项检查 | `GET /api/v1/blacklist/authority/{authorId}` |

### 2.2 全局黑名单（global）
| 项目 | 说明 |
|------|------|
| Redis Key | `blacklist:global`（Set 结构） |
| 数据来源 | 管理员手动添加 |
| 有效期 | 永久 |
| 优先级 | 中等（第二检查） |
| CRUD | `GET/POST/DELETE /api/v1/blacklist/global` |
| 单项检查 | `GET /api/v1/blacklist/global/{authorId}` |

### 2.3 临时黑名单（temp）
| 项目 | 说明 |
|------|------|
| Redis Key | `blacklist:temp:{authorId}`（String 结构） |
| 数据来源 | 检测结果 HIGH 时自动添加；也可手动添加 |
| 有效期 | 24 小时（86400 秒），通过 SETEX 设置 |
| 优先级 | 最低（最后检查） |
| CRUD | `GET/POST/DELETE /api/v1/blacklist/temp` |
| 单项检查 | `GET /api/v1/blacklist/temp/{authorId}`（返回剩余 TTL 秒数） |

### 2.4 联合查询
- `GET /api/v1/blacklist/check/{authorId}` — 按优先级依次检查三层黑名单，返回第一个命中的来源和原因

---

## 三、检测历史查询

- **接口：** `GET /api/v1/history`
- **必填参数：** `deviceId`
- **可选参数：** `afterId`（游标）、`page`（页码，默认 1）、`size`（页大小，默认 20，范围 1-100）

### 双模式分页

| 模式 | 触发条件 | 说明 |
|------|----------|------|
| 游标分页（增量同步） | 传入 `afterId` | 查询 `id > afterId` 的记录，内部取 `size+1` 判断 `hasMore` |
| 偏移分页（翻页浏览） | 不传 `afterId` | 标准 `LIMIT offset, size` 分页 |

### 响应字段
- `records` — 历史记录列表（含 id、deviceId、authorId、riskLevel、score、createdAt）
- `total` — 该设备的总记录数
- `hasMore` — 是否还有更多数据
- `latestId` — 当前批次最大 ID，可作为下次 `afterId` 使用

---

## 四、安全认证

### API Key 认证

- **认证方式：** 请求头 `X-API-Key`
- **实现：** 自定义 `ApiKeyFilter`（继承 `OncePerRequestFilter`），集成 Spring Security
- **会话策略：** `STATELESS`（无状态），CSRF 已禁用
- **放行路径：** `/actuator/**` 无需认证
- **业务路径：** 需携带有效 API Key，获得 `ROLE_API_CLIENT` 权限
- **认证失败：** 返回 HTTP 401，JSON 体 `{"code":401,"message":"Missing or invalid API key"}`
- **配置项：** `api.key`（环境变量 `API_KEY`，开发环境默认 `dev-api-key-123`）

---

## 五、健康检查与监控

### Actuator 端点
- 暴露端点：`/actuator/health`、`/actuator/info`（无需认证）
- 配置：`management.endpoints.web.exposure.include: health,info`

### AI 服务健康指标（AIHealthIndicator）
- 自定义 `HealthIndicator`，检查 Python AI 服务连通性
- 向 AI 服务根路径发送 GET 请求，任何 HTTP 响应（含 404）均视为 UP
- 仅连接级别失败才报告 DOWN

---

## 六、数据持久化

### MySQL 数据库
- **数据库名：** `aiscanner`（utf8mb4 字符集）
- **唯一业务表：** `t_detection_record`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT AUTO_INCREMENT | 主键，单调递增 |
| device_id | VARCHAR(128) | 设备标识 |
| author_id | VARCHAR(128) | 作者标识 |
| risk_level | VARCHAR(16) | 风险等级（HIGH/MEDIUM/SAFE） |
| score | DOUBLE | 风险加权分数 |
| raw_ai_result | TEXT | AI 原始返回 JSON（审计留痕） |
| created_at | DATETIME | 创建时间 |

- **索引：** `device_id`、`author_id`、`risk_level`、`created_at`
- **ORM：** MyBatis，驼峰自动映射，XML 配置在 `classpath:mapper/*.xml`
- **连接池：** HikariCP（min-idle=5, max=20）

### Redis 缓存
- **连接池：** Lettuce（max-active=8, max-idle=8, min-idle=2）
- **用途：** 三层黑名单数据存储

---

## 七、统一响应与异常处理

### ApiResponse 统一响应体
```json
{
  "code": 200,
  "message": "success",
  "data": { ... }
}
```
- null 字段自动省略（`@JsonInclude(NON_NULL)`）
- 工厂方法：`ok(data)`、`ok()`、`error(code, message)`

### 全局异常处理（GlobalExceptionHandler）

| 异常类型 | HTTP 状态码 | 说明 |
|----------|-------------|------|
| MaxUploadSizeExceededException | 400 | 视频文件过大 |
| MissingServletRequestParameterException | 400 | 缺少必填参数 |
| MultipartException | 400 | 请求未包含视频文件 |
| IllegalArgumentException | 400 | 参数不合法 |
| Exception（兜底） | 500 | 内部服务器错误（日志记录堆栈） |

---

## 八、配置管理

### 主配置（application.yml）
- 服务端口：8080（环境变量 `SERVER_PORT`）
- 文件上传：最大 10MB
- MySQL：`serverTimezone=Asia/Shanghai`，密码/Redis 主机等通过环境变量注入
- AI 服务：地址 `AI_SERVICE_URL`（默认 `http://localhost:8001`），超时 30 秒

### 开发环境（application-dev.yml）
- AI 服务地址：`http://localhost:8000`
- API Key：`dev-api-key-123`
- 日志级别：DEBUG（`org.example.aiscanner_server` 包）
