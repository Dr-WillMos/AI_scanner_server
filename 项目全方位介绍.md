# 短视频反诈后端系统 — AIscanner Server 全方位介绍

> **版本**: 1.0-SNAPSHOT | **技术栈**: Spring Boot 3.5.14 + Java 25 + Vue 3 | **最后更新**: 2026-06-12

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术栈](#2-技术栈)
3. [系统架构](#3-系统架构)
4. [核心业务流程](#4-核心业务流程)
5. [数据库设计](#5-数据库设计)
6. [API 接口文档](#6-api-接口文档)
7. [安全体系](#7-安全体系)
8. [异步处理与死信队列](#8-异步处理与死信队列)
9. [前端管理后台](#9-前端管理后台)
10. [配置说明](#10-配置说明)
11. [部署方案](#11-部署方案)
12. [测试策略](#12-测试策略)
13. [运维监控](#13-运维监控)
14. [本地开发指南](#14-本地开发指南)

---

## 1. 项目概述

### 1.1 项目定位

AIscanner Server 是一个**短视频反欺诈后端服务**，主要功能是接收短视频（MP4 格式），调用外部 Python AI 服务进行分析，综合评估风险等级，并对高风险发布者实施黑名单管理。

### 1.2 核心能力

| 能力 | 说明 |
|---|---|
| **视频检测** | 同步/异步两种模式，支持 MP4 格式验证（MIME + 文件签名） |
| **AI 分析** | 调用 Python AI 服务检测视频中的虚假信息、暴力内容、关键词命中 |
| **风险评分** | 基于 AI 结果的加权评分公式（反欺诈概率 70% + 暴力概率 30%） |
| **黑名单管理** | 三级黑名单体系（权威/全局/临时），Redis + MySQL 双重存储 |
| **API Key 管理** | 设备自注册 + 管理员 CRUD，基于角色的权限控制 |
| **速率限制** | Redis 固定窗口计数器，按 API Key 粒度限制 |
| **死信队列** | Redis Stream 实现，支持消息重试、指数退避、超限告警 |
| **管理后台** | Vue 3 单页应用，仪表盘、记录查询、黑名单管理、密钥管理、健康监控 |

### 1.3 适用场景

- 短视频平台的内容安全审核
- 反欺诈/反虚假信息自动化检测
- 发布者信誉管理与黑名单维护

---

## 2. 技术栈

### 2.1 后端

| 组件 | 版本 | 用途 |
|---|---|---|
| Java | 25 | 运行语言 |
| Spring Boot | 3.5.14 | 应用框架 |
| Spring MVC | — | REST API 层 |
| Spring Security | — | 认证授权 |
| Spring Data Redis | — | Redis 集成（Lettuce 客户端） |
| Spring Boot Actuator | — | 健康检查 + Micrometer 指标 |
| MyBatis | 3.0.4 (spring-boot-starter) | 数据库 ORM |
| MySQL Connector/J | — | MySQL 8.0+ 驱动 |
| Flyway | —（Spring Boot 托管） | 数据库版本迁移 |
| Micrometer + Prometheus | — | 应用指标采集 |
| Logback | — | 日志框架 |
| Jackson | —（Spring Boot 托管） | JSON 序列化 |
| HikariCP | —（Spring Boot 托管） | 数据库连接池 |

### 2.2 基础设施

| 组件 | 版本 | 用途 |
|---|---|---|
| MySQL | 8.0+ | 主数据库 |
| Redis | 6.0+ / 7-alpine (Docker) | 缓存 / 速率限制 / 异步队列 / 任务状态 |
| Python AI Service | 外部服务 | 视频内容 AI 分析 |

### 2.3 前端

| 组件 | 版本 | 用途 |
|---|---|---|
| Vue 3 | 3.5.13 | 前端框架（Composition API） |
| TypeScript | — | 类型安全 |
| Vite | 6.0.7 | 构建工具 |
| Element Plus | 2.9.2 | UI 组件库 |
| ECharts | 5.5.1 | 数据可视化（饼图/柱状图） |
| Pinia | 2.3.0 | 状态管理 |
| Axios | 1.7.9 | HTTP 客户端 |
| Vue Router | — | 前端路由（Hash 模式） |

---

## 3. 系统架构

### 3.1 整体架构图

```
                          ┌──────────────────────────────────────┐
                          │    aiscanner-admin (Vue 3 SPA)        │
                          │    Port 5173 (dev) / Nginx (prod)      │
                          └────────────────┬─────────────────────┘
                                           │ X-API-Key Header
                          ┌────────────────▼─────────────────────┐
                          │         CORS Filter                    │
                          │    (app.cors.allowed-origins)           │
                          └────────────────┬─────────────────────┘
                                           │
                          ┌────────────────▼─────────────────────┐
                          │      ApiKeyFilter (@Order=1)           │
                          │   • Root Key → 全部权限 + 无限速       │
                          │   • 动态 Key → Redis 缓存 → MySQL      │
                          │   • 设置请求属性供下游使用              │
                          └────────────────┬─────────────────────┘
                                           │
                          ┌────────────────▼─────────────────────┐
                          │    RateLimitFilter (@Order=2)          │
                          │   • 固定窗口计数器 (Redis INCR)         │
                          │   • /actuator/** + admin keys 豁免     │
                          │   • 超限返回 429 + Retry-After         │
                          └────────────────┬─────────────────────┘
                                           │
                          ┌────────────────▼─────────────────────┐
                          │    Spring Security URL ACL             │
                          │    (基于 hasRole 的 URL 级别鉴权)       │
                          └────────────────┬─────────────────────┘
                                           │
              ┌──────────┬──────────┬──────┴──────┬──────────┬───────────┐
              ▼          ▼          ▼             ▼          ▼           ▼
         /detect    /detect/   /history      /blacklist   /keys       /dlq
         (sync)     async      /stats        (3 tiers)    (CRUD)      (管理)
              │          │
              │          └──► Redis Stream ──► DetectStreamConsumer
              │                    │
              ▼                    ▼
        DetectionService   DetectTaskService
              │                    │
              ├── BlacklistService (短路检查)
              ├── AiClient (Python AI)
              ├── RiskCalculator (评分)
              ├── DetectionRecordMapper (MySQL)
              └── DetectionMetrics (Micrometer)
```

### 3.2 模块划分

| 包路径 | 职责 |
|---|---|
| `client/` | 外部服务调用（AI 服务 HTTP 客户端） |
| `common/` | 通用组件（统一响应体、异常处理、业务异常） |
| `config/` | Spring 配置类（安全、Redis、速率限制） |
| `controller/` | REST 控制器（6 个 Controller） |
| `health/` | Actuator 健康指示器（AI 服务可达性） |
| `mapper/` | MyBatis Mapper 接口（3 个） |
| `metrics/` | Micrometer 指标定义 |
| `model/dto/` | 数据传输对象（请求/响应模型） |
| `model/entity/` | 数据库实体 |
| `model/enums/` | 枚举（RiskLevel、TaskStatus） |
| `security/` | 安全过滤器（ApiKeyFilter、RateLimitFilter） |
| `service/` | 业务逻辑层（12 个 Service） |
| `stream/` | Redis Stream 生产者/消费者/DLQ 重试 |

### 3.3 关键技术决策

**为何选择 Redis Stream 而非 RocketMQ？**

项目早期引入了 RocketMQ 依赖（`pom.xml` 中已注释），最终改用 Redis Stream：
- 减少基础设施组件数量（仅需 MySQL + Redis）
- Redis Stream 满足当前吞吐量需求
- 运维成本更低

**为何设计三级黑名单？**

| 层级 | 存储 | 生命周期 | 管理方式 |
|---|---|---|---|
| 权威 (authority) | Redis Set + MySQL | 永久 | 管理员手动维护 |
| 全局 (global) | Redis Set + MySQL | 永久 | 管理员手动维护 |
| 临时 (temp) | Redis String (24h TTL) | 24 小时 | 系统自动添加 |

优先级：authority > global > temp，命中即短路，无需继续检查。

---

## 4. 核心业务流程

### 4.1 同步检测流程（POST /api/v1/detect）

```
客户端提交 (deviceId, authorId, video.mp4)
         │
         ▼
  ┌─────────────────┐
  │ VideoValidator    │  验证: 非空 / Content-Type=video/mp4 / .mp4后缀 / ftyp魔数
  └────────┬────────┘
           │ 通过
           ▼
  ┌─────────────────┐
  │ BlacklistService  │  检查 authorId 三级黑名单 → 命中直接返回 HIGH
  └────────┬────────┘
           │ 未命中
           ▼
  ┌─────────────────┐
  │ AiClient          │  POST /v1/analyze/video (multipart)
  │                   │  重试: 最多 3 次 (1s/2s/4s 退避)
  │                   │  4xx → 立即失败, 5xx/连接 → 重试
  └────────┬────────┘
           │ 返回 (keywordHit, aiGlitchProb, violenceProb, transcription)
           ▼
  ┌─────────────────┐
  │ RiskCalculator    │  keywordHit → HIGH (score=1.0)
  │                   │  否则: score = 0.7*aiGlitchProb + 0.3*violenceProb
  │                   │  ≥0.6 → HIGH  |  ≥0.3 → MEDIUM  |  <0.3 → SAFE
  └────────┬────────┘
           │
           ▼
  ┌─────────────────┐
  │ 保存检测记录       │  INSERT INTO t_detection_record
  │ 记录 Micrometer   │
  └────────┬────────┘
           │
           ▼
  ┌─────────────────┐
  │ HIGH → 自动加入   │  blacklist:temp:{authorId} (SETEX 24h)
  │      临时黑名单   │
  └────────┬────────┘
           │
           ▼
      返回 DetectResponse
```

### 4.2 异步检测流程（POST /api/v1/detect/async）

```
客户端提交 (deviceId, authorId, video.mp4)
         │
         ├── VideoValidator.validate()
         ├── VideoStorageService.save(taskId, video)  →  /tmp/aiscanner/videos/{taskId}.mp4
         ├── DetectTaskService.createTask(taskId, deviceId, authorId)  →  Redis HASH, 24h TTL
         ├── DetectStreamProducer.send()  →  Redis Stream "detect:stream"
         └── 返回 202 + TaskSubmitResponse(taskId, PENDING, createdAt)

                     ═══════════════ 异步边界 ═══════════════

DetectStreamConsumer (轮询 2s)
         │
         ├── 读取 Stream 消息
         ├── 更新任务状态 → PROCESSING
         ├── VideoStorageService.read(taskId)
         ├── DetectionService.detect()  (同同步流程)
         ├── 更新任务状态 → DONE (result) 或 FAILED (error)
         ├── VideoStorageService.delete(taskId)
         │
         └── 失败处理:
               retryCount < 3 → Producer.requeue()  (加回主 Stream)
               retryCount ≥ 3 → Producer.sendToDlq()  (送入 DLQ Stream)

状态查询: GET /api/v1/detect/{taskId}/status → 读取 Redis detect:task:{taskId}
```

### 4.3 风险评分公式

```
score = 0.7 × aiGlitchProb + 0.3 × violenceProb

关键词命中 (keywordHit=true) → 直接 HIGH (score=1.0)

score ≥ 0.6  →  HIGH   (综合评分过高)
score ≥ 0.3  →  MEDIUM (综合评分中等)
score < 0.3  →  SAFE
```

AI 服务返回的三个核心指标：
- `aiGlitchProb` — 视频中虚假/诈骗内容的概率（权重 70%）
- `violenceProb` — 暴力内容概率（权重 30%）
- `keywordHit` — 是否命中反诈关键词（一票否决）

---

## 5. 数据库设计

### 5.1 t_detection_record（检测记录表）

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT (PK, AUTO_INCREMENT) | 主键 |
| `device_id` | VARCHAR(128) NOT NULL | 提交检测的设备 ID |
| `author_id` | VARCHAR(128) NOT NULL | 抖音作者号 |
| `risk_level` | VARCHAR(16) NOT NULL | 风险等级：HIGH / MEDIUM / SAFE |
| `score` | DOUBLE NOT NULL DEFAULT 0.0 | 风险加权分数 |
| `raw_ai_result` | TEXT | AI 服务原始返回 JSON（完整保留） |
| `created_at` | DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |

索引：`idx_device_id`, `idx_author_id`, `idx_risk_level`, `idx_created_at`

### 5.2 t_api_key（API Key 管理表）

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT (PK, AUTO_INCREMENT) | 主键 |
| `key_value` | VARCHAR(64) UNIQUE NOT NULL | API Key 值（UUID 32位去横线） |
| `key_name` | VARCHAR(128) NOT NULL | Key 描述（设备名/用途） |
| `device_id` | VARCHAR(128) | 绑定的设备 ID（可为空） |
| `permissions` | VARCHAR(256) NOT NULL DEFAULT 'DETECT,HISTORY' | 权限列表（逗号分隔） |
| `status` | VARCHAR(16) NOT NULL DEFAULT 'ACTIVE' | ACTIVE / REVOKED |
| `rate_limit` | INT NOT NULL DEFAULT 20 | 每分钟最大请求数（0=不限速） |
| `last_used_at` | DATETIME | 最后使用时间 |
| `expired_at` | DATETIME | 过期时间（NULL=永不过期） |
| `created_at` | DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `revoked_at` | DATETIME | 吊销时间 |

索引：`uk_key_value` (UNIQUE), `idx_device_id`, `idx_status`

### 5.3 t_blacklist（黑名单表 — Flyway V2）

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | BIGINT (PK, AUTO_INCREMENT) | 主键 |
| `author_id` | VARCHAR(128) NOT NULL | 作者号 |
| `list_type` | VARCHAR(16) NOT NULL | AUTHORITY 或 GLOBAL |
| `reason` | VARCHAR(255) DEFAULT '' | 加黑原因 |
| `created_at` | DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |

唯一约束：`uk_author_list (author_id, list_type)` — 同一作者在同一类型列表中只能出现一次

### 5.4 Redis 键空间设计

| Key 模式 | 类型 | TTL | 说明 |
|---|---|---|---|
| `apikey:{keyValue}` | String (JSON) | 5 min | API Key 缓存 |
| `ratelimit:{keyValue}:{windowStart}` | String (counter) | 窗口结束 | 速率限制计数器 |
| `detect:task:{taskId}` | Hash | 24 h | 异步任务状态 |
| `detect:stream` | Stream | — | 异步任务主队列 |
| `detect:stream:dlq` | Stream | — | 死信队列 |
| `blacklist:authority` | Set | 永久 | 权威黑名单（内存，MySQL 为准） |
| `blacklist:global` | Set | 永久 | 全局黑名单（内存，MySQL 为准） |
| `blacklist:temp:{authorId}` | String | 24 h | 临时黑名单（自动过期） |

---

## 6. API 接口文档

> 所有接口统一返回 `ApiResponse<T>` 格式：`{"code":200, "message":"success", "data":...}`

### 6.1 检测接口（需要 ROLE_DETECT）

#### POST /api/v1/detect — 同步检测

```
Content-Type: multipart/form-data
Header: X-API-Key: <key>

参数:
  deviceId (String)  — 设备 ID
  authorId (String)  — 抖音作者号
  video    (File)    — MP4 视频文件 (≤10MB)

响应:
{
  "code": 200,
  "data": {
    "riskLevel": "HIGH",
    "score": 0.85,
    "reason": "综合评分过高",
    "transcription": "...",
    "blacklisted": false
  }
}
```

#### POST /api/v1/detect/async — 异步检测

```
参数: 同上

响应 (202):
{
  "code": 200,
  "data": {
    "taskId": "uuid",
    "status": "PENDING",
    "createdAt": "2026-06-12T10:30:00"
  }
}
```

#### GET /api/v1/detect/{taskId}/status — 查询异步任务状态

```
响应:
{
  "code": 200,
  "data": {
    "taskId": "uuid",
    "status": "PROCESSING",     // PENDING | PROCESSING | DONE | FAILED
    "result": { ... },           // DONE 时存在
    "error": "...",              // FAILED 时存在
    "createdAt": "...",
    "updatedAt": "..."
  }
}
```

### 6.2 历史查询接口（需要 ROLE_HISTORY）

#### GET /api/v1/history

```
参数:
  deviceId  (String, 可选)  — 设备 ID 过滤
  authorId  (String, 可选)  — 作者号过滤
  riskLevel (String, 可选)  — HIGH/MEDIUM/SAFE 过滤
  startDate (String, 可选)  — 开始日期 (yyyy-MM-dd)
  endDate   (String, 可选)  — 结束日期 (yyyy-MM-dd)
  page      (int, 默认 1)
  size      (int, 默认 20, 上限 100)
  afterId   (long, 可选)    — 游标分页 (优先级高于 page)

响应:
{
  "code": 200,
  "data": {
    "items": [ ... ],
    "total": 150,
    "page": 1,
    "size": 20,
    "hasMore": true,
    "nextCursor": 123
  }
}
```

### 6.3 黑名单接口（需要 ROLE_ADMIN）

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/api/v1/blacklist/{type}` | 列出某类黑名单（type: authority/global/temp） |
| `GET` | `/api/v1/blacklist/{type}/{authorId}` | 检查 authorId 是否在某类黑名单中 |
| `POST` | `/api/v1/blacklist/{type}` | 添加 authorId 到某类黑名单（body: {authorId, reason}） |
| `DELETE` | `/api/v1/blacklist/{type}/{authorId}` | 从某类黑名单中移除 |
| `GET` | `/api/v1/blacklist/check/{authorId}` | 聚合检查 — 返回在哪类黑名单中 |
| `GET` | `/api/v1/blacklist/temp/{authorId}` | 查询临时黑名单及剩余 TTL |

### 6.4 API Key 管理接口

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| `POST` | `/api/v1/keys/register` | 公开 | 设备自注册 |
| `GET` | `/api/v1/keys` | ROLE_ADMIN | 列出所有 Key（不含 keyValue） |
| `GET` | `/api/v1/keys/{id}` | ROLE_ADMIN | 获取 Key 详情（含 keyValue） |
| `POST` | `/api/v1/keys` | ROLE_ADMIN | 手动创建 Key |
| `PUT` | `/api/v1/keys/{id}` | ROLE_ADMIN | 更新 Key 配置 |
| `POST` | `/api/v1/keys/{id}/revoke` | ROLE_ADMIN | 吊销 Key（可逆） |
| `DELETE` | `/api/v1/keys/{id}` | ROLE_ADMIN | 删除 Key（不可逆） |

### 6.5 死信队列接口（需要 ROLE_ADMIN）

| 方法 | 路径 | 说明 |
|---|---|---|
| `GET` | `/api/v1/dlq` | 列出 DLQ 消息（默认 50，上限 100） |
| `GET` | `/api/v1/dlq/{messageId}` | 获取单条 DLQ 消息详情 |
| `POST` | `/api/v1/dlq/{messageId}/retry` | 重试某条 DLQ 消息 |
| `DELETE` | `/api/v1/dlq/{messageId}` | 删除某条 DLQ 消息 |
| `GET` | `/api/v1/dlq/stats` | DLQ 统计（待处理数、最终死信数、最旧消息时间） |
| `DELETE` | `/api/v1/dlq/purge` | 清空 DLQ |

### 6.6 统计与健康接口

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| `GET` | `/api/v1/stats` | ROLE_ADMIN | 仪表盘统计数据 |
| `GET` | `/actuator/health` | 公开 | 健康检查（含 DB/Redis/AI 组件） |
| `GET` | `/actuator/info` | 公开 | 应用信息 |
| `GET` | `/actuator/metrics` | 公开 | Micrometer 指标 |
| `GET` | `/actuator/prometheus` | 公开 | Prometheus 格式指标 |

---

## 7. 安全体系

### 7.1 认证流程

```
请求进入
    │
    ▼
ApiKeyFilter (@Order=1)
    │
    ├── 路径是 /actuator/** 或 POST /api/v1/keys/register ?
    │       → Yes: 放行 (不做认证)
    │
    ├── 提取 Header: X-API-Key
    │       → 缺失: 返回 401
    │
    ├── 匹配 Root Key (api.key 配置)?
    │       → Yes: 设置权限 ROLE_DETECT + ROLE_HISTORY + ROLE_ADMIN
    │              设置 rateLimit=0 (不限速)
    │
    └── 否则: 查询 Redis 缓存 (apikey:{key})
            → 未命中: 查询 MySQL (t_api_key)
            → 验证 status=ACTIVE 且未过期
            → 设置权限 (依据 permissions 字段)
            → 设置 rateLimit (依据 rate_limit 字段)
            → 缓存到 Redis (TTL 5min)
            → 更新 last_used_at
```

### 7.2 权限模型

| 角色 | 包含权限 | 典型用户 |
|---|---|---|
| `ROLE_DETECT` | 提交检测、查询任务状态 | 普通设备 |
| `ROLE_HISTORY` | 查询历史记录 | 运营人员 |
| `ROLE_ADMIN` | 黑名单管理、Key 管理、DLQ 管理、统计 | 管理员 |

Root Key 拥有全部三种角色。动态 Key 的权限由 `permissions` 字段的逗号分隔值决定（如 `DETECT,HISTORY`）。

### 7.3 URL 安全矩阵

| URL | Method | 所需角色 |
|---|---|---|
| `/actuator/**` | * | 公开 |
| `/api/v1/detect` | POST | ROLE_DETECT |
| `/api/v1/detect/async` | POST | ROLE_DETECT |
| `/api/v1/detect/*/status` | GET | ROLE_DETECT |
| `/api/v1/history` | GET | ROLE_HISTORY |
| `/api/v1/blacklist/**` | * | ROLE_ADMIN |
| `/api/v1/keys/register` | POST | 公开 |
| `/api/v1/keys/**` | * | ROLE_ADMIN |
| `/api/v1/stats` | GET | ROLE_ADMIN |
| `/api/v1/dlq/**` | * | ROLE_ADMIN |

### 7.4 速率限制

- **算法**: 固定窗口计数器（Redis `INCR` + `EXPIRE`）
- **粒度**: 按 API Key（窗口 Key = `ratelimit:{keyValue}:{windowStart}`）
- **默认限制**: 每分钟 20 次（可配置）
- **管理 Key**: `rate_limit=0` → 不限速
- **响应头**: `X-RateLimit-Limit`, `X-RateLimit-Remaining`, `X-RateLimit-Reset`, `Retry-After`
- **降级策略**: Redis 不可用时放行所有请求

### 7.5 安全加固

| 配置 | 值 |
|---|---|
| CSRF | 已禁用（无状态 API） |
| Session | STATELESS |
| HSTS | max-age=31536000, includeSubDomains |
| X-Content-Type-Options | nosniff |
| X-Frame-Options | DENY |
| XSS Protection | 启用 |
| CORS | 仅允许配置的域名 |

---

## 8. 异步处理与死信队列

### 8.1 Redis Stream 架构

```
Producer (DetectStreamProducer)
    │
    ├── send() → XADD detect:stream * taskId deviceId authorId filePath retryCount
    │
    └── sendToDlq() → XADD detect:stream:dlq * taskId deviceId authorId filePath error retryCount

Consumer (DetectStreamConsumer)
    │
    ├── 消费组: detect-consumers
    ├── 消费者: consumer-1
    ├── 轮询超时: 2s
    ├── onMessage() → 处理检测 → XACK
    └── 失败 → retryCount < 3 ? requeue() : sendToDlq()

DLQ 重试 (DlqRetryScheduler)
    │
    ├── @Scheduled(fixedDelay = 30s)
    ├── XREADGROUP detect:stream:dlq
    ├── 指数退避: 60s → 300s → 900s → 1800s
    └── 超过 4 次 DLQ 重试 → 最终死信

DLQ 告警 (DlqAlertService)
    │
    ├── @Scheduled(fixedRate = 5min)
    └── pending ≥ 10 条 → 日志 WARN (30min 冷却)
```

### 8.2 重试策略对比

| 阶段 | 位置 | 最大重试 | 退避策略 |
|---|---|---|---|
| 主队列重试 | detect:stream | 3 次 | 立即 requeue |
| DLQ 重试 | detect:stream:dlq | 4 次 | 60s / 300s / 900s / 1800s |
| 最终死信 | 留在 DLQ Stream | — | 等待人工介入 |

---

## 9. 前端管理后台

### 9.1 技术架构

```
aiscanner-admin/
├── package.json          # 依赖管理
├── tsconfig.json         # TypeScript 配置
├── vite.config.ts        # Vite 构建 + 代理配置
└── src/
    ├── main.ts           # 入口 (Pinia + Router + Element Plus)
    ├── App.vue           # 根组件
    ├── router/index.ts   # Hash 路由 + 登录守卫
    ├── stores/auth.ts    # Pinia 认证状态 (localStorage 持久化)
    ├── api/
    │   ├── client.ts     # Axios 实例 (拦截器: X-API-Key / 401 / 429)
    │   ├── blacklist.ts  # 黑名单 API
    │   ├── detections.ts # 检测记录 API
    │   ├── dlq.ts        # DLQ API
    │   ├── health.ts     # 健康检查 API
    │   ├── keys.ts       # Key 管理 API
    │   └── stats.ts      # 统计 API
    ├── components/
    │   ├── AppLayout.vue # 侧边栏 + 顶栏布局
    │   └── RiskBadge.vue # 风险等级标签组件
    └── views/
        ├── LoginView.vue       # 登录页 (API Key 验证)
        ├── DashboardView.vue   # 仪表盘 (统计卡片 + ECharts)
        ├── DetectionsView.vue  # 检测记录页 (筛选 + 分页表格)
        ├── BlacklistView.vue   # 黑名单管理 (三标签页)
        ├── KeysView.vue        # Key 管理页 (CRUD + 吊销)
        ├── HealthView.vue      # 健康监控 (15s 自动刷新)
        └── DlqView.vue        # 死信队列 (30s 自动刷新)
```

### 9.2 页面功能一览

| 页面 | 路由 | 核心功能 |
|---|---|---|
| 登录 | `/login` | API Key 输入，验证通过后保存到 localStorage |
| 仪表盘 | `/dashboard` | 4 张统计卡片 + 风险分布饼图 + 黑名单柱状图 |
| 检测记录 | `/detections` | 多条件筛选表格，支持游标/偏移分页 |
| 黑名单 | `/blacklist` | 三标签页（权威/全局/临时），单条+批量操作 |
| Key 管理 | `/keys` | 表格 + 创建/详情/编辑/吊销/删除对话框 |
| 健康监控 | `/health` | MySQL/Redis/AI 三张状态卡片，自动刷新 |
| 死信队列 | `/dlq` | 消息列表 + 详情/重试/删除/清空操作 |

### 9.3 前端运行

```bash
cd aiscanner-admin
npm install
npm run dev          # 启动开发服务器 → http://localhost:5173
```

Vite 自动将 `/api` 和 `/actuator` 代理到 `http://localhost:8080`。

---

## 10. 配置说明

### 10.1 环境变量

| 变量 | 默认值 | 说明 |
|---|---|---|
| `DB_PASSWORD` | `root` | MySQL 数据库密码 |
| `REDIS_HOST` | `localhost` | Redis 主机地址 |
| `REDIS_PORT` | `6379` | Redis 端口 |
| `REDIS_PASSWORD` | (空) | Redis 密码 |
| `AI_SERVICE_URL` | `http://localhost:8001` | Python AI 服务地址 |
| `API_KEY` | `changeme` | Root API Key |
| `SERVER_PORT` | `8080` | 服务端口 |
| `CORS_ORIGINS` | `http://localhost:5173` | 允许的跨域来源（逗号分隔） |

### 10.2 Profile 配置

| Profile | 文件 | 用途 |
|---|---|---|
| `default` | `application.yml` | 默认配置，使用环境变量占位符 |
| `dev` | `application-dev.yml` | 开发环境（明文密码、DEBUG 日志、AI 超时 120s） |
| `docker` | `application-docker.yml` | Docker 部署（服务名替代 localhost、视频目录 /data/videos） |

### 10.3 启动命令

```bash
# 开发环境
./mvnw spring-boot:run -Dspring-boot.run.profiles=dev

# 生产环境（JAR）
java -jar target/AIscanner_server-1.0-SNAPSHOT.jar --spring.profiles.active=prod

# Docker
docker compose up -d
```

---

## 11. 部署方案

### 11.1 Docker Compose（推荐）

```yaml
services:
  mysql:    # MySQL 8.0，数据卷持久化
  redis:    # Redis 7-alpine，AOF 持久化
  app:      # Spring Boot 应用，依赖 mysql/redis 健康检查通过
```

```bash
# 准备环境变量
cp .env.example .env
# 编辑 .env 填入实际值

# 启动
docker compose up -d

# 查看日志
docker compose logs -f app

# 停止
docker compose down
```

### 11.2 Dockerfile（多阶段构建）

```
Stage 1 (builder): maven:3.9-eclipse-temurin-25
    → 离线下载依赖
    → 编译打包 (mvn clean package -DskipTests)

Stage 2 (runtime): eclipse-temurin:25-jre-alpine
    → 创建非 root 用户 appuser
    → 复制 JAR
    → 创建视频存储目录 /data/videos
    → EXPOSE 8080
    → ENTRYPOINT java -jar app.jar
```

### 11.3 数据库初始化

```bash
# 手动初始化数据库
mysql -u root -p < src/main/resources/sql/init.sql

# 或依赖 Flyway 自动迁移
# 应用启动时自动执行 src/main/resources/db/migration/V*.sql
```

---

## 12. 测试策略

### 12.1 Java 测试

| 类型 | 注解 | 示例文件 | 覆盖内容 |
|---|---|---|---|
| **单元测试** | `@ExtendWith(MockitoExtension.class)` | `RiskCalculatorTest` | 风险评分边界值、关键词优先 |
| **单元测试** | `@ExtendWith(MockitoExtension.class)` | `VideoValidatorTest` | MP4 格式验证（正常/空/类型错误/签名错误） |
| **单元测试** | `@ExtendWith(MockitoExtension.class)` | `BlacklistServiceTest` | 三级黑名单命中优先级 |
| **单元测试** | `@ExtendWith(MockitoExtension.class)` | `DetectionServiceTest` | 检测流程 + 黑名单短路 |
| **单元测试** | `@ExtendWith(MockitoExtension.class)` | `HistoryServiceTest` | 游标/偏移分页 |
| **单元测试** | `@ExtendWith(MockitoExtension.class)` | `ApiKeyFilterTest` | 过滤器逻辑（反射设置 rootKey） |
| **Web 切片** | `@WebMvcTest` | — | 导入 Security + ApiKeyFilter |
| **集成测试** | `@SpringBootTest` | `SecurityIntegrationTest` | 完整过滤器链 + 权限 |
| **集成测试** | `@SpringBootTest` | `ActuatorHealthIntegrationTest` | 健康端点 + 组件状态 |

运行测试：

```bash
./mvnw clean test                          # 运行全部测试
./mvnw test -Dtest=RiskCalculatorTest      # 运行单个测试类
```

### 12.2 Python 端到端测试

项目还包含 Python 测试套件：

| 文件 | 用途 |
|---|---|
| `mock_ai_server.py` | Flask 模拟 AI 服务，支持场景切换 |
| `test_aiscanner.py` | unittest 全链路测试 |

模拟服务器支持通过 `authorId` 前缀切换场景：
- `safe_*` → 返回安全检测结果
- `medium_*` → 返回中等风险
- `high_*` → 返回高风险
- `keyword_*` → 返回关键词命中
- `bl_*` → 命中黑名单

---

## 13. 运维监控

### 13.1 健康检查

```
GET /actuator/health

{
  "status": "UP",
  "components": {
    "db": { "status": "UP", "details": { "database": "MySQL" } },
    "redis": { "status": "UP", "details": { "version": "7.x" } },
    "aiService": { "status": "UP" }
  }
}
```

- `db` — MySQL 连接检查
- `redis` — Redis 连接检查
- `aiService` — AI 服务可达性（任何 HTTP 响应即 UP，仅连接失败时 DOWN）

### 13.2 Micrometer 指标

| 指标名称 | 类型 | 说明 |
|---|---|---|
| `aiscanner.detection.count` | Counter | 检测次数（tag: riskLevel） |
| `aiscanner.blacklist.hit` | Counter | 黑名单命中次数 |
| `aiscanner.rate.limit.exceeded` | Counter | 速率限制超限次数 |
| `aiscanner.ai.call.duration` | Timer | AI 服务调用耗时 |

通过 Prometheus 格式暴露：
```
GET /actuator/prometheus
```

### 13.3 日志

- 框架：Logback
- 配置：`src/main/resources/logback-spring.xml`
- 输出：控制台 + 滚动文件（`logs/aiscanner.log`）
- 日志级别：默认 INFO，dev profile 为 DEBUG

---

## 14. 本地开发指南

### 14.1 环境要求

- **JDK 25** — 编译和运行
- **MySQL 8.0+** — 主数据库
- **Redis 6.0+** — 缓存/队列
- **Node.js 18+** — 前端构建（可选）
- **Maven** — 使用项目自带的 `mvnw`（Maven Wrapper）

### 14.2 后端开发流程

```bash
# 1. 初始化数据库
mysql -u root -p -e "CREATE DATABASE IF NOT EXISTS aiscanner"
# Flyway 启动时自动建表

# 2. 启动 Redis
redis-server

# 3. 启动 Mock AI 服务（可选，用于本地调试）
python mock_ai_server.py    # 监听 localhost:8001

# 4. 启动后端
./mvnw spring-boot:run -Dspring-boot.run.profiles=dev

# 验证
curl http://localhost:8080/actuator/health
```

### 14.3 前端开发流程

```bash
cd aiscanner-admin
npm install
npm run dev          # http://localhost:5173
```

### 14.4 常用 API 测试命令

```bash
# 设置 API Key 变量
API_KEY="changeme"

# 同步检测
curl -X POST http://localhost:8080/api/v1/detect \
  -H "X-API-Key: $API_KEY" \
  -F "deviceId=test-device" \
  -F "authorId=test-author" \
  -F "video=@test_video.mp4"

# 查询历史
curl http://localhost:8080/api/v1/history \
  -H "X-API-Key: $API_KEY"

# 查询统计
curl http://localhost:8080/api/v1/stats \
  -H "X-API-Key: $API_KEY"
```

### 14.5 项目结构快速索引

```
src/main/java/org/example/aiscanner_server/
├── AiScannerServerApplication.java    — 启动入口
├── client/AiClient.java               — AI 服务 HTTP 客户端
├── common/
│   ├── ApiResponse.java               — 统一响应体
│   ├── AiServiceException.java        — AI 服务异常
│   └── GlobalExceptionHandler.java    — 全局异常处理
├── config/
│   ├── RateLimitProperties.java       — 速率限制配置属性
│   ├── RedisConfig.java               — Redis 模板配置
│   ├── RedisStreamConfig.java         — Stream 消费者组初始化
│   ├── RestClientConfig.java          — RestClient Bean
│   └── SecurityConfig.java            — Spring Security 配置
├── controller/
│   ├── ApiKeyController.java          — Key 管理
│   ├── BlacklistController.java       — 黑名单管理
│   ├── DetectionController.java       — 检测入口
│   ├── DlqController.java             — DLQ 管理
│   ├── HistoryController.java         — 历史查询
│   └── StatsController.java           — 统计查询
├── health/AIHealthIndicator.java      — AI 健康指示器
├── mapper/
│   ├── ApiKeyMapper.java              — t_api_key Mapper
│   ├── BlacklistMapper.java           — t_blacklist Mapper
│   └── DetectionRecordMapper.java     — t_detection_record Mapper
├── metrics/DetectionMetrics.java      — Micrometer 指标
├── model/
│   ├── dto/                           — 14 个数据传输对象
│   ├── entity/                        — 3 个数据库实体
│   └── enums/                         — RiskLevel, TaskStatus
├── security/
│   ├── ApiKeyFilter.java              — API Key 认证过滤器
│   └── RateLimitFilter.java           — 速率限制过滤器
├── service/                           — 12 个业务服务
└── stream/
    ├── DetectStreamConsumer.java      — Stream 消费者
    ├── DetectStreamProducer.java      — Stream 生产者
    └── DlqRetryScheduler.java         — DLQ 定时重试
```

